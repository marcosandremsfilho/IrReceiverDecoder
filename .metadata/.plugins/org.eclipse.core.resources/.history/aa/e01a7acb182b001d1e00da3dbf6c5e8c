#include "main.h"

#ifndef SENSORIRCLASS
#define SENSORIRCLASS

class SensorIR {
protected:
	uint16_t timeON, timeOFF, time, lockInicial = 0, lockFinal = 0, ignoreFirstSignal = 0, ignoreSecondSignal = 0;
	uint32_t decodeSignal, Signal, timeSignal[32], timeSignalSONY[16];
	int flagOn = 0, flagOff = 0, flagCode, bit, primeiro, segundo;
	int position = 0, protocol, decodePosition;
	enum {NEC, Sony};
public:
	SensorIR() { };
	void getTime();
	void decodeNECSignalFunction();
};




#endif /* INC_SENSORIRCLASS_H_ */
