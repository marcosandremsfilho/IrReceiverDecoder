#include "main.h"

#ifndef SENSORIRCLASS
#define SENSORIRCLASS

class SensorIR {
protected:
	uint16_t timeON, timeOFF, time, lockInicial = 0;
	uint32_t decodeSignal, Signal, timeSignal[32], timeSignalSONY[16];
	int flagOn = 0, flagOff = 0, flagCode, bit;
	int position = 0;
public:
	SensorIR() { };
	void getTime();
	void decodeSignalFunction();
};




#endif /* INC_SENSORIRCLASS_H_ */
